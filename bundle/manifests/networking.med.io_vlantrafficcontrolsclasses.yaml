apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  creationTimestamp: null
  name: vlantrafficcontrolsclasses.networking.med.io
spec:
  group: networking.med.io
  names:
    kind: VlanTrafficControlClass
    listKind: VlanTrafficControlClassList
    plural: vlantrafficcontrolsclasses
    shortNames:
    - vtcclass
    - vtcclasses
    singular: vlantrafficcontrolclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.className
      name: CLASS_NAME
      type: string
    - jsonPath: .spec.direction
      name: DIRECTION
      type: string
    - jsonPath: .spec.classId
      name: CLASS_ID
      type: string
    - jsonPath: .spec.vlanId
      name: VLAN_ID
      type: integer
    - jsonPath: .spec.guaranteed
      name: GUARANTEED
      type: string
    - jsonPath: .spec.ceilBorrow
      name: CEIL_BORROW
      type: string
    - jsonPath: .spec.ingressRate
      name: INGRESS_RATE
      type: string
    - jsonPath: .spec.ingressCeil
      name: INGRESS_CEIL
      type: string
    - jsonPath: .spec.aligned
      name: ALIGNED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              aligned:
                description: Aligned is the alignment status confirming runtime kernel
                  state matches this spec ('True' or 'False').
                type: string
              ceilBorrow:
                description: CeilBorrow is the maximum ceiling rate allowed for dynamic
                  egress bandwidth borrowing.
                type: string
              classId:
                description: ClassID is the resolved HTB class ID handle (e.g., '1:100',
                  '1:380').
                type: string
              className:
                description: ClassName is the human-readable class name referenced
                  from parent VlanTrafficControl CR.
                type: string
              direction:
                description: Direction is the traffic flow direction for this class
                  ('ingress', 'egress', or 'ingress+egress').
                type: string
              dscp:
                description: Dscp is the target DSCP code point associated with this
                  class projection.
                type: integer
              egressBurst:
                description: EgressBurst is the configured egress burst size buffer.
                type: string
              enableFqCodel:
                description: EnableFqCodel indicates if FQ_CoDel Active Queue Management
                  is enabled on this class.
                type: boolean
              guaranteed:
                description: Guaranteed is the guaranteed bandwidth rate allocated
                  for egress traffic.
                type: string
              ingressAction:
                description: IngressAction is the action taken when ingress rate is
                  exceeded.
                enum:
                - drop
                - pass
                type: string
              ingressBurst:
                description: IngressBurst is the configured ingress burst size buffer.
                type: string
              ingressCeil:
                description: IngressCeil is the maximum ceiling rate allowed for ingress
                  IFB traffic.
                type: string
              ingressRate:
                description: IngressRate is the guaranteed bandwidth rate allocated
                  for ingress traffic.
                type: string
              ip:
                description: IP is the target IP address associated with this class
                  projection.
                type: string
              mark:
                description: Mark is the FWMark value associated with this class projection.
                format: int32
                type: integer
              matchType:
                description: MatchType is the traffic classification rule type used
                  for matching.
                type: string
              port:
                description: Port is the target TCP/UDP port associated with this
                  class projection.
                type: integer
              priority:
                description: Priority is the filter evaluation priority assigned to
                  this class rule.
                type: integer
              subnet:
                description: Subnet is the IP Subnet CIDR associated with this class
                  projection.
                type: string
              vlanId:
                description: VlanID is the VLAN ID associated with this class projection.
                type: integer
            required:
            - classId
            - className
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
